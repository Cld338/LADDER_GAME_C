#ifndef LADDER_PLAYER_H
#define LADDER_PLAYER_H


#define MAX_INPUT 100
#include "ladder.h"
#include "word.h"
int run(int (*map)[ARR_SIZE][ARR_SIZE], struct WORDLIST*, int cho_count);

#endif